name: My MCP Servers
version: 0.0.1
schema: v1

mcpServers:
  - name: PostgreSQL
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-postgres"
      - "postgresql://postgres:postgres@localhost:5432/de_lab"
